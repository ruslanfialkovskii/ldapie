#!/usr/bin/env python3
# -*- coding: utf-8 -*-

import os
import sys

# Add the parent directory to the Python path if running from repository
parent_dir = os.path.dirname(os.path.abspath(__file__))
sys.path.insert(0, parent_dir)

try:
    # First try to import as if the package is installed
    from ldapie.ldapie import cli
except ImportError:
    # Fall back to the development path
    from src.ldapie.ldapie import cli

if __name__ == "__main__":
    cli()
